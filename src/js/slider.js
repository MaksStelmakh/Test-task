$(document).ready(function () {
  $('.multiple-items').slick({
    infinite: true,
    slidesToScroll: 1,
    arrows: true,
    variableWidth: true,
    autoplay: true,
    autoplaySpeed: 2000,
  });
});
